import { UrlActivePipe } from './url-active.pipe';

describe('UrlActivePipe', () => {
  it('create an instance', () => {
    const pipe = new UrlActivePipe();
    expect(pipe).toBeTruthy();
  });
});
